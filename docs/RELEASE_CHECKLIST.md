# リリース前チェックリスト

公開前に「誰が」「何を」確認すれば済むかを 1 枚にまとめたもの。
手順の詳細は [DEPLOY.md](DEPLOY.md)、背景と根拠は
[review/COMPLETION_PLAN.md](review/COMPLETION_PLAN.md) を参照。

凡例: ✅ 済(この環境で確認できるもの)/ ⬜ 未(本番環境が必要)/ 🅰 甲の支給待ち

---

## 1. コードの品質ゲート(✅ すべて緑)

`main` へのマージ前に CI が通ることを確認する。手元では以下で同じことを確かめられる。

| 確認               | コマンド                                                                                 | 現状        |
| ------------------ | ---------------------------------------------------------------------------------------- | ----------- |
| 型                 | `pnpm typecheck`                                                                         | ✅ エラー 0 |
| Lint               | `pnpm lint`                                                                              | ✅ エラー 0 |
| 整形               | `pnpm format:check`                                                                      | ✅ 差分 0   |
| ユニットテスト     | `pnpm test`                                                                              | ✅ 278 件   |
| ビルド             | `pnpm build`                                                                             | ✅ 成功     |
| `setup-hosted.sql` | `node scripts/gen-setup-hosted.mjs && git diff --exit-code -- supabase/setup-hosted.sql` | ✅ 差分 0   |

E2E はローカルの Supabase スタックが必要。

```bash
pnpm db:start                       # Docker が必要
pnpm db:reset
node scripts/seed-users.mjs         # テスト会員
node scripts/seed-dev.mjs 120       # ダミー商品
node scripts/seed-images.mjs        # 商品画像
pnpm dev                            # 別ターミナル
E2E_BASE_URL=http://localhost:3000 pnpm test:e2e
```

`pnpm test:e2e` は Chromium のみ。WebKit / Firefox の主要導線は
`pnpm exec playwright install webkit firefox` のあと `pnpm test:e2e:cross`
(CI では `e2e-webkit` ジョブが WebKit を受け持つ)。

`scripts/*.mjs` と E2E は接続先がローカルでないと止まる
(`--allow-remote` / `CYCLEX_ALLOW_REMOTE=1` を明示した場合のみ通る)。

現状 **27 件すべて緑**。結果は
[plan/ACCEPTANCE_RESULT.md](plan/ACCEPTANCE_RESULT.md) の「再実施の記録」に記録済み。

---

## 2. 環境変数(⬜ 本番で設定)

`src/lib/env.ts` の `REQUIRED_IN_PRODUCTION` が本番起動時に検証する。
欠けていれば `src/instrumentation.ts` が起動時に落とすので、
「動いているのに設定が抜けている」状態にはならない。

| 変数                            | 用途                       | 備考                                        |
| ------------------------------- | -------------------------- | ------------------------------------------- |
| `NEXT_PUBLIC_SUPABASE_URL`      | Supabase 接続              |                                             |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase 接続(公開可)      |                                             |
| `SUPABASE_SERVICE_ROLE_KEY`     | サーバー側の管理操作       | **共有しない**                              |
| `STRIPE_SECRET_KEY`             | Checkout の作成・失効      | 本番キー。テストキーと取り違えないこと      |
| `STRIPE_WEBHOOK_SECRET`         | Webhook の署名検証         | Webhook 登録後に発行される値                |
| `RESEND_API_KEY`                | メール送信                 | 未設定だと送信せず `skipped` ログのみ       |
| `EMAIL_FROM`                    | 送信元                     | 🅰 甲のドメイン                              |
| `CRON_SECRET`                   | 日次バッチの認証           | 推測されない値                              |
| `CYCLEX_PAYMENTS_DISABLED`      | 決済を閉じて公開する       | Stripe を後回しにする場合のみ `1`。下記参照 |
| `NEXT_PUBLIC_APP_URL`           | メール内リンク・OGP の基準 | 設定後に**再デプロイが必要**                |

- `ALLOW_DEMO_CHECKOUT` は**本番では絶対に設定しない**(決済を通さず取引が成立する)。
  `src/lib/env.ts` が本番で立っていたら起動を止める
- 検証公開なら `NEXT_PUBLIC_NOINDEX=1`(`robots.txt` が全面拒否になる)

### Stripe を後回しにする場合(`CYCLEX_PAYMENTS_DISABLED=1`)

Stripe のキー発行を待たずに、閲覧・会員登録・出品・メッセージ・管理までを
関係者に見せたいときは `CYCLEX_PAYMENTS_DISABLED=1` を入れる。
`STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` が必須から外れ、代わりに

- 商品詳細の CTA が「決済の準備中です」になる(押せない)
- 購入手続き画面を直接開いても案内が出て、支払いボタンが押せない
- `startPurchase` が Action を直接叩かれても断る(取引の行を作らない)
- `/api/webhooks/stripe` が 503 で明示的に断る
- 起動ログに「決済を無効にして起動しました」と残る

**このフラグを入れずに Stripe のキーを空で本番デプロイすると、ビルドは通るが
起動時に落ちて全ページが 500 になる**(検証は `instrumentation.ts` の起動時)。
逆に適当な値を入れると「購入手続きへ」まで進めたうえで Checkout の作成に失敗する。
どちらも避けるためのフラグなので、決済を後回しにするなら必ず入れる。

決済を有効にするときは、Stripe のキー 2 つを入れて**このフラグを消す**。
フラグが残っていると決済は閉じたままになる(起動ログで気づける)。

この挙動はローカルで `VERCEL_ENV=production` + `next start` を使って確認済み
(2026-09-10)。フラグ無しでは全ルートが 500、フラグ有りでは主要ルートが 200 で
上記 5 点すべてが期待どおりだった。

---

## 3. データベース(⬜ 本番で適用)

| #   | 作業                                                 | 確認方法                                                                                                                                       |
| --- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 3-1 | `supabase/setup-hosted.sql` を本番プロジェクトへ適用 | `supabase_migrations.schema_migrations` に 10 本入る                                                                                           |
| 3-2 | 拡張 `pg_trgm` の有効化                              | `select show_trgm('ロードバイク');` が返る                                                                                                     |
| 3-3 | Storage バケット 3 つ                                | `setup-hosted.sql` が作るので手作業は不要。`listing-images`(公開)/ `avatars`(公開)/ `listing-images-hidden`(**非公開**)の 3 件を目視で確認する |
| 3-4 | バックアップ方針                                     | Pro の PITR、または週次 `pg_dump` の運用手順を決める                                                                                           |
| 3-5 | 管理者アカウント                                     | 🅰 甲の指定アドレスで登録し `users.role='admin'` に更新                                                                                         |

`listing-images-hidden` が無いと、運営が商品を非表示にしても画像が公開 URL に
残る(退避先が無いため)。作成漏れは管理操作のエラーメッセージで気づけるようにしてある。

**アプリより先に SQL を当てること。** レート制限は `consume_rate_limit`
関数で判定し、判定できないときは操作を通さない(fail-closed)。
関数が無い状態でアプリを動かすと、出品・メッセージ・通報・ログインが
すべて「混雑しています」で断られる。

**`setup-hosted.sql` は更地に一度だけ**。2 回目を流すと先頭のガードが
「既に適用されています」で止める(何も変更しない)。以後のスキーマ変更は
`supabase link` → `pnpm db:push` で当てる。

この手順は本番と同じ形で予行済み。ローカルの Supabase を更地に戻して
`setup-hosted.sql` を貼り、次を確認した(2026-09-10)。

| 確認                   | 結果                                               |
| ---------------------- | -------------------------------------------------- |
| 適用                   | エラー 0 件で完走                                  |
| `schema_migrations`    | 10 本。マイグレーションのファイル名と一致          |
| テーブル               | 13 本、**全 13 本で RLS 有効**                     |
| ポリシー / 関数 / 索引 | 25 / 42 / 56                                       |
| Storage バケット       | 3 件。`listing-images-hidden` が非公開になっている |
| `pg_trgm`              | `show_trgm('ロードバイク')` が返る                 |
| 初期データ             | ブランド 30 件                                     |
| 2 回目の実行           | ガードで停止し、Storage のポリシー 4 本が残る      |

---

## 4. 認証(⬜ 本番で確認)

| #   | 作業                                           | 確認方法                                                 |
| --- | ---------------------------------------------- | -------------------------------------------------------- |
| 4-1 | Site URL / Redirect URLs を本番ドメインに      | 確認メールのリンクが本番へ飛ぶ                           |
| 4-2 | メールテンプレートを `{{ .TokenHash }}` 形式に | 登録した端末とメールを開く端末が違っても通る             |
| 4-3 | 会員登録 → 確認メール → ログイン               | 実機で 1 周                                              |
| 4-4 | パスワード再設定 → `/reset-password/update`    | 着地点が `/mypage` になっていないこと(§7 E-1 の回帰確認) |
| 4-5 | メールアドレス変更                             | 新旧どちらにも確認メールが届く                           |
| 4-6 | Google ログイン                                | 🅰 Google Cloud のクライアント ID                         |
| 4-7 | セッションの寿命                               | Pro の Session timebox を使うかは判断待ち(§5 #3)         |

---

## 5. 決済(⬜ 本番で確認 / 後回しなら §2 のフラグ)

> Stripe を後回しにする場合、この節は `CYCLEX_PAYMENTS_DISABLED=1` を外して
> キーを入れる段階でまとめて実施する。それまでは購入の導線が閉じているため、
> 5-1 以降は確認できない(確認しようとしても「決済の準備中です」で止まる)。

Webhook に登録するイベント(6 種):

```
checkout.session.completed
checkout.session.expired
checkout.session.async_payment_succeeded
checkout.session.async_payment_failed
charge.dispute.created
charge.refunded
```

| #   | 確認                                        | 期待                                                                                        |
| --- | ------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 5-1 | テストカードで購入                          | 取引が `paid`、商品が `trading`、双方にメール                                               |
| 5-2 | 決済画面から戻る                            | 取引がキャンセルされ、商品がすぐ購入可能に戻る                                              |
| 5-3 | 期限切れ(45 分放置 または `expired` を送る) | 取引がキャンセル、商品が復帰                                                                |
| 5-4 | 同じ Webhook を再送                         | 二重処理されない(`already_processed`)                                                       |
| 5-5 | 署名を壊して送る                            | 400 が返る                                                                                  |
| 5-6 | キャンセル後に入金(§7 C-2)                  | 運営に「要返金」、**購入者にも**「返金します」が届く                                        |
| 5-7 | 進行中の取引を Stripe で返金(§7 H-4)        | 取引は進行中のまま、運営に確認依頼が届く。`refunded_at` は立たない                          |
| 5-8 | 強制キャンセル(§7 C-1)                      | `pending_payment` の取引で「強制キャンセル」が使え、履歴に `canceled_without_expire` が残る |

5-6 〜 5-8 は今回の監査で追加した経路なので、本番でも一度は通しておく。

---

## 6. メール(⬜ / 🅰)

| #   | 作業                              | 確認方法                                                                                                                 |
| --- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 6-1 | Resend のドメイン認証(SPF / DKIM) | 🅰 甲のドメイン                                                                                                           |
| 6-2 | 16 種すべての到達確認             | 迷惑メール判定されないことも見る                                                                                         |
| 6-3 | 通知設定 OFF の反映               | OFF のカテゴリが届かず、`email_logs` に `skipped` が残る                                                                 |
| 6-4 | 設定で止められない 5 種           | `welcome` / `tx_paid_after_cancel` / `admin_dispute` / `admin_late_payment` / `admin_refund_review` は設定に関わらず届く |

---

## 7. 定期実行(⬜)

| #   | 確認                                         | 期待                                                                                             |
| --- | -------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 7-1 | Vercel Cron が日次で走る                     | ログに実行記録                                                                                   |
| 7-2 | `CRON_SECRET` 無しで叩く                     | 401                                                                                              |
| 7-3 | 未決済の掃除                                 | 45 分以上前の `pending_payment` が消える。**失敗が続く場合はログに件数と取引 ID が出る**(§7 C-1) |
| 7-4 | 評価の 14 日自動公開・評価ゼロ取引の自動完了 | 対象が処理される                                                                                 |

---

## 8. 公開前の目視(⬜ 一部 ✅)

| #   | 確認                                          | 現状                                            |
| --- | --------------------------------------------- | ----------------------------------------------- |
| 8-1 | 375 / 768 / 1280 / 1920px で主要 26 画面      | ✅ 375 / 1280px は撮影して確認。1920px は本番で |
| 8-2 | 横スクロールとタップ領域                      | ✅ E2E で 320 / 375 / 768 / 1280px              |
| 8-3 | PC の絞り込みサイドバーが最後まで辿れる       | ✅ 実測で修正済み(§8.3)                         |
| 8-4 | 一般会員で `/admin` が 404                    | ✅ E2E                                          |
| 8-5 | 未ログインで会員ページ → ログインへ誘導       | ✅ E2E                                          |
| 8-6 | 非表示にした商品の画像が公開 URL から見えない | ⬜ 本番の Storage で確認(D-9 と併せて)          |

---

## 9. 甲の支給物(🅰)

これが揃わないと公開できない(特商法表記は法的前提)。

- [ ] 特定商取引法に基づく表記
- [ ] 利用規約
- [ ] プライバシーポリシー
- [ ] 問い合わせ窓口(`/suspended` と購入完了画面にも導線が必要)
- [ ] `EMAIL_FROM` の実値とドメイン
- [ ] ブランドマスタの確認
- [ ] 管理者アカウントのアドレス
- [ ] Stripe アカウント / Google Cloud クライアント ID

---

## 10. 公開後すぐに見るもの

- Vercel のログで `[production env]` の警告が出ていないか
- `email_logs` に `failed` が溜まっていないか
- `/admin/transactions?refund=pending` が空か(空でなければ返金対応が必要)
- Stripe ダッシュボードの Webhook 配信失敗
- 日次バッチのログに `[cleanup]` の失敗件数が出ていないか
